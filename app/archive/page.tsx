import { BentoDashboard } from "@/components/bento";

export const metadata = {
  title: "The Sam Archive",
  description: "9 Active Artifacts — Curated by Sam",
};

export default function ArchivePage() {
  return <BentoDashboard />;
}
